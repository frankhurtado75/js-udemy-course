// Const: variable moderna
const producto = 'Tablet';

// No se pueden reasignar
console.log(producto);
producto = 'Monitor 23 Pulgadas'; // (error)

// Las variables con const deben iniciarse con un valor (error)
const precio;
precio = 20;
console.log(precio);