// En lugar de crear diferentes variables
const nombre = "Monitor 20 Pulgadas";
const precio = 300;
const disponible = true;

// Object literal, un objeto agrupa todo en una sola variable
const producto = { // las llaves despues del signo = representan un objeto

    // Esto 'nombre' se le llama una propiedad o llave del objeto
    nombre: "Monitor 20 Pulgadas", 

    // En lugar del signo igual se utilizan :
    precio: 300, 

    // El último elemento puede o no tener la ,
    disponible: true 
}

console.log(producto);